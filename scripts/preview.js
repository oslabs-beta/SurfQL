//document on load

document.addEventListener('DOMContentLoaded', ()=> {
    //get board element
    const board = document.querySelector('#board');

    //add a static message
    board.innerHTML = 'Grow a beautiful tree';

    //build board content with graphql schema
});
